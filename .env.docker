# Environment variables for Docker deployment
# AudioForge uses MemStorage (in-memory) + LocalAudioStorage (browser) - no database required

# Application
NODE_ENV=production
PORT=5000

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-in-production-minimum-32-characters

# Audio Processing Configuration
MAX_FILE_SIZE=100MB
UPLOAD_DIR=/app/uploads

# Security Settings
CORS_ORIGIN=http://localhost:5000

# Logging
LOG_LEVEL=info

# Performance Settings
MAX_AUDIO_DURATION=7200  # 2 hours in seconds
MAX_TRACKS=10
MAX_CLIPS_PER_TRACK=100

# Feature Flags
ENABLE_AUDIO_PROCESSING=true
ENABLE_FILE_UPLOAD=true
ENABLE_PROJECT_SAVE=true

# Note: Application uses:
# - Backend: MemStorage (in-memory, resets on container restart)
# - Frontend: LocalAudioStorage (browser localStorage/IndexedDB)
# - Files: Volume-mounted uploads directory for persistence